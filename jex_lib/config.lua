JexConfig = {
    -- Only set this if you run a fork under a different resource name.
    Core = 'auto',

    Debug = false,

    Accounts = {
        cash = 'cash',
        bank = 'bank',
        gold = 'gold',
    },
}
